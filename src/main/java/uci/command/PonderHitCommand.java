package uci.command;

import engine.core.state.EngineState;

public class PonderHitCommand implements Command {
    @Override
    public int execute(EngineState engineState) {
        // TODO implement ponderhit command
        return 0;
    }
}
