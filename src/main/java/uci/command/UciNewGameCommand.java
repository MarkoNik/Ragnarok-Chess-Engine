package uci.command;

import engine.state.EngineState;

public class UciNewGameCommand implements Command {
    @Override
    public int execute(EngineState engineState) {
        // TODO reset game state
        return 0;
    }
}
